name: CI Pipeline
on: [push]
jobs:
    unit-tests:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - run: bash ./runTests.sh
    api-tests:
        runs-on: ubuntu-latest
        steps:
            - uses: cypress-io/github-action@v4
            - run: npx cypress
